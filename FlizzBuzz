class Solution:
    def fizzBuzz(self, n: int) -> List[str]:
        lists=[]
        for numbers in range(1,n+1):
            if numbers%15==0:
                lists.append("FizzBuzz")
            elif (numbers%3)==0:
                lists.append("Fizz")
            elif numbers%5==0:
                lists.append("Buzz")
            else:
                lists.append(str(numbers))
        return lists
